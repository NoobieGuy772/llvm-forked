name: Super-Trimmed LLVM Engine + Cross-Sysroots

on:
  workflow_dispatch:

jobs:
  build:
    name: Build LLVM on ${{ matrix.os }}
    strategy:
      fail-fast: false # Ensures other OS builds continue if one fails
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      # 1. Install Dependencies
      - name: Install Dependencies
        shell: bash
        run: |
          if [[ "${{ runner.os }}" == "Linux" ]]; then
            sudo apt update && sudo apt install -y ninja-build cmake build-essential ccache zip mingw-w64 lld
          elif [[ "${{ runner.os }}" == "macOS" ]]; then
            brew install cmake ninja ccache
          elif [[ "${{ runner.os }}" == "Windows" ]]; then
            choco install -y cmake ninja ccache zip llvm
            echo "C:\Program Files\LLVM\bin" >> $GITHUB_PATH
          fi

      # 2. Setup Caching
      - name: Restore Cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/ccache
            ~/.ccache
            ~/AppData/Local/ccache
          key: ${{ runner.os }}-llvm-fast-v3-${{ github.run_id }}
          restore-keys: ${{ runner.os }}-llvm-fast-v3-

      # 3. Prepare Sysroots
      - name: Build Linux musl sysroot
        if: runner.os == 'Linux'
        run: |
          git clone --depth=1 https://github.com/ifduyue/musl
          cd musl && ./configure --prefix=$PWD/../sysroot-linux
          make -j$(nproc) && make install

      - name: Prepare Windows MinGW sysroot
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          $PathsToTry = @("C:\mingw64", "C:\ProgramData\Chocolatey\lib\mingw\tools\install\mingw64", "C:\tools\mingw64")
          foreach ($path in $PathsToTry) {
              if (Test-Path "$path\bin") {
                  mkdir sysroot-windows -Force
                  xcopy /E /I /H /Y "$path\*" "sysroot-windows\"
                  break
              }
          }

      - name: Prepare macOS SDK sysroot
        if: runner.os == 'macOS'
        run: |
          SDK_PATH=$(xcrun --show-sdk-path)
          mkdir -p sysroot-macos
          rsync -a --copy-links --exclude='*.swiftmodule' "$SDK_PATH/" sysroot-macos/ || true

      # 4. FAST BUILD: Minimum targets and LLD linker
      - name: Build LLVM Engine
        shell: bash
        run: |
          git clone --depth=1 https://github.com/llvm/llvm-project.git
          mkdir -p build && cd build
          
          LINKER_FLAG="-DLLVM_USE_LINKER=lld"
          if [[ "${{ runner.os }}" == "macOS" ]]; then LINKER_FLAG=""; fi

          cmake -G Ninja ../llvm-project/llvm \
            -DCMAKE_BUILD_TYPE=Release \
            -DLLVM_ENABLE_PROJECTS="clang;lld" \
            -DLLVM_TARGETS_TO_BUILD="X86;AArch64" \
            $LINKER_FLAG \
            -DLLVM_ENABLE_ASSERTIONS=OFF \
            -DLLVM_ENABLE_BACKTRACES=OFF \
            -DLLVM_ENABLE_TERMINFO=OFF \
            -DLLVM_INCLUDE_TESTS=OFF \
            -DLLVM_INCLUDE_EXAMPLES=OFF \
            -DLLVM_INCLUDE_BENCHMARKS=OFF \
            -DLLVM_INCLUDE_UTILS=OFF \
            -DLLVM_ENABLE_ZLIB=OFF \
            -DLLVM_ENABLE_ZSTD=OFF \
            -DLLVM_ENABLE_LIBXML2=OFF \
            -DLLVM_ENABLE_BINDINGS=OFF \
            -DLLVM_OPTIMIZED_TABLEGEN=ON \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_INSTALL_PREFIX=../install
          
          ninja install

      # 5. Smart Packaging & Specific Extension Cleanup
      - name: Package
        shell: bash
        run: |
          mkdir -p pkg/llvm/bin pkg/llvm/lib
          cd install/bin
          for tool in clang clang++ lld ld.lld llc opt llvm-config; do
            cp -a ${tool}* ../../pkg/llvm/bin/ 2>/dev/null || true
          done
          cd ../..

          cp -r install/lib/clang pkg/llvm/lib/ 2>/dev/null || true
          cp -r install/include pkg/llvm/ 2>/dev/null || true

          if [[ "${{ runner.os }}" != "Windows" ]]; then
            find pkg/llvm/bin -type f -exec strip {} + || true
            find pkg/llvm -type f \( -name "*.deb" -o -name "*.pkg" \) -delete
          else
            find pkg/llvm -type f \( -name "*.pdb" -o -name "*.exp" -o -name "*.lib" \) -delete
          fi

          [ -d sysroot-linux ] && cp -r sysroot-linux pkg/
          [ -d sysroot-windows ] && cp -r sysroot-windows pkg/
          [ -d sysroot-macos ] && cp -r sysroot-macos pkg/

          zip -r llvm-cross-engine-${{ matrix.os }}.zip pkg/

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: llvm-engine-${{ matrix.os }}
          path: llvm-cross-engine-${{ matrix.os }}.zip

  release:
    needs: build
    if: always() && contains(needs.build.result, 'success')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4 # Crucial for gh CLI to find the repo context

      - uses: actions/download-artifact@v4
        with:
          path: ./release-files
          merge-multiple: true

      - name: Create Auto-Incremented Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # gh CLI now works because checkout provided the git context
          LATEST_TAG=$(gh release list --limit 1 | awk '{print $1}')
          [ -z "$LATEST_TAG" ] && LATEST_TAG="llvm0"
          
          NUMBER=$(echo $LATEST_TAG | sed 's/[^0-9]*//g')
          [ -z "$NUMBER" ] && NUMBER=0
          NEW_TAG="llvm$((NUMBER + 1))"
          
          if [ -d "./release-files" ] && [ "$(ls -A ./release-files)" ]; then
            gh release create "$NEW_TAG" ./release-files/*.zip \
              --title "LLVM Engine Trimmed ($NEW_TAG)" \
              --notes "Optimized Release build. "
          else
            echo "No artifacts found. Skipping release."
            exit 1
          fi
